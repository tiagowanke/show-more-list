import { ShowMoreListDirective } from './show-more-list.directive';

describe('ShowMoreListDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMoreListDirective();
    expect(directive).toBeTruthy();
  });
});
